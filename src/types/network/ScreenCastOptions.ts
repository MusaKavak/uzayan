export type ScreenCastOptions = {
    width: string,
    height: string,
    x: string,
    y: string,
    framerate: string,
    port: string
}

export type Screen = {
    name: string,
    width: string,
    height: string,
}