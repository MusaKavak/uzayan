export type ScreenCastOptions = {
    screen: string
    width: string,
    height: string,
    x: string,
    y: string,
    port: string
}

export type Screen = {
    name: string,
    width: string,
    height: string,
}